<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <h2>Subrutas disponibles:</h2>
    <ul>
        <% subroutes.forEach(subroute => { %>
            <li><a href="<%= subroute %>"><%= subroute %></a></li>
        <% }); %>
    </ul>
    <p><a href="/">Volver a la página principal</a></p>
</body>
</html>