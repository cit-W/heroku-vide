<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RESTAURANTE</title>
    <style>
        :root {
            --primary-bg: #171717;
            --secondary-bg: #1a1a1a;
            --border-color: #2c2c2c;
            --hover-border: #666666;
            --hover-bg: #292929;
            --text-color: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--primary-bg);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            padding: 20px;
        }

        h1 {
            margin: 30px 0;
            font-size: clamp(2rem, 5vw, 3.5rem);
            font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
            text-align: center;
            color: var(--text-color);
        }
        
        .boton-elegante {
            display: block;
            width: 100%;
            max-width: 400px;
            padding: 15px 20px;
            margin: 15px auto;
            border: 2px solid var(--border-color);
            background-color: var(--secondary-bg);
            color: var(--text-color);
            font-size: clamp(1rem, 2vw, 1.2rem);
            cursor: pointer;
            border-radius: 30px;
            transition: all 0.3s ease;
            outline: none;
            position: relative;
            overflow: hidden;
            font-weight: bold;
        }

        .boton-elegante::after {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(
                circle,
                rgba(255, 255, 255, 0.25) 0%,
                rgba(255, 255, 255, 0) 70%
            );
            transform: scale(0);
            transition: transform 0.5s ease;
        }

        .boton-elegante:hover::after {
            transform: scale(4);
        }

        .boton-elegante:hover {
            border-color: var(--hover-border);
            background: var(--hover-bg);
        }

        .boton-elegante:active {
            transform: scale(0.95);
        }
        
        main {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: clamp(20px, 5vw, 40px);
            margin: 20px auto;
            width: min(90%, 800px);
            border-radius: 32px;
            background: linear-gradient(225deg, #1e1e1e, #232323);
            box-shadow: -35px 35px 70px #1c1c1c,
                        35px -35px 70px #262626;
        }

        @media (max-width: 768px) {
            body {
                padding: 10px;
            }
            
            main {
                padding: 20px;
                margin: 10px auto;
                border-radius: 20px;
                box-shadow: -15px 15px 30px #1c1c1c,
                            15px -15px 30px #262626;
            }

            .boton-elegante {
                padding: 12px 20px;
                margin: 10px auto;
            }
        }
    </style>
</head>
<body>
    <h1>RESTAURANTE</h1>
    <main>
        <button class="boton-elegante" id="btn_mensual" data-route="/restaurante/upload">
            Subir lista general de pagos mensuales</button>
        <button class="boton-elegante" id="btn_estudiante" data-route="/restaurante/update-pago-form">
            Actualizar pago estudiante</button>
        <button class="boton-elegante" id="btn_hoy" data-route="/restaurante/estadisticas/asistencia-dia?hoy=true">
            Estadísticas de hoy</button>
        <button class="boton-elegante" id="btn_estadisticas_mes" data-route="/restaurante/estadisticas/asistencia-mes">
            Estadísticas del mes</button>
        <button class="boton-elegante" id="btn_deudores" data-route="/restaurante/upload">
            Deudores</button>
    </main>
    <script>
        document.querySelectorAll('.boton-elegante').forEach(button => {
            button.addEventListener('click', () => {
                window.location.href = button.dataset.route;
            });
        });
    </script>
</body>
</html>