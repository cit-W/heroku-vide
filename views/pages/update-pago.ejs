<!DOCTYPE html>
<html>
<head>
    <title>Actualizar Pago Mensual</title>
    <style>
        body { font-family: Arial; max-width: 600px; margin: 2rem auto; padding: 0 1rem; }
        .form-group { margin-bottom: 1rem; }
        label { display: block; margin-bottom: 0.5rem; }
        input, select { width: 100%; padding: 0.5rem; }
        button { background: #4CAF50; color: white; padding: 0.5rem 1rem; border: none; cursor: pointer; }
        .message { padding: 1rem; margin: 1rem 0; border-radius: 4px; }
        .success { background: #dff0d8; }
        .error { background: #f2dede; }
    </style>
</head>
<body>
    <h1>Actualizar Pago Mensual</h1>
    
    <% if (locals.message) { %>
        <div class="message <%= locals.error ? 'error' : 'success' %>">
            <%= message %>
        </div>
    <% } %>

    <form action="/update-pago" method="POST">
        <div class="form-group">
            <label for="id">ID del Estudiante:</label>
            <input type="text" id="id" name="id" required>
        </div>

        <div class="form-group">
            <label for="pago_mensual">Estado de Pago:</label>
            <select id="pago_mensual" name="pago_mensual" required>
                <option value="true">Pagado</option>
                <option value="false">No Pagado</option>
            </select>
        </div>

        <button type="submit">Actualizar Pago</button>
    </form>
</body>
</html>