import request from 'supertest';import { expect } from 'chai';import app from 'file:///C:/Users/jhoan/Documents/heroku-vide/app.js';import sinon from 'sinon';import pool from 'file:///C:/Users/jhoan/Documents/heroku-vide/config/db.js';describe('Teacher Schedules Routes', () => {  let sandbox;  let mockClient;  beforeEach(() => {    sandbox = sinon.createSandbox();    mockClient = {      query: sandbox.stub(),      release: sandbox.stub(),    };    sandbox.stub(pool, 'connect').resolves(mockClient);  });  afterEach(() => {    sandbox.restore();  });  describe('GET /teacher-schedules/create-schedule', () => {    it('should create a schedule successfully', async () => {      mockClient.query.resolves({});      const res = await request(app)        .get('/teacher-schedules/create-schedule')        .query({          Hora: '8:00-9:00',          LunesHora: 'Math',          MartesHora: 'Science',          MiercolesHora: 'History',          JuevesHora: 'Art',          ViernesHora: 'Music',          nombre: 'ProfesorA',        });      expect(res.statusCode).to.equal(200);      expect(res.text).to.equal('Horario registrado');    });    it('should return 400 if required data is missing', async () => {      const res = await request(app)        .get('/teacher-schedules/create-schedule')        .query({          Hora: '8:00-9:00',          LunesHora: 'Math',          MartesHora: 'Science',          MiercolesHora: 'History',          JuevesHora: 'Art',          ViernesHora: 'Music',        });      expect(res.statusCode).to.equal(400);      expect(res.text).to.equal('Todos los parámetros son requeridos.');    });    it('should return 500 if schedule creation fails', async () => {      mockClient.query.throws(new Error('DB Error'));      const res = await request(app)        .get('/teacher-schedules/create-schedule')        .query({          Hora: '8:00-9:00',          LunesHora: 'Math',          MartesHora: 'Science',          MiercolesHora: 'History',          JuevesHora: 'Art',          ViernesHora: 'Music',          nombre: 'ProfesorA',        });      expect(res.statusCode).to.equal(500);    });  });  describe('GET /teacher-schedules/create-teacher-table', () => {    it('should create a teacher table successfully', async () => {      mockClient.query.resolves({});      const res = await request(app)        .get('/teacher-schedules/create-teacher-table?profe=ProfesorA');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.true;      expect(res.body.message).to.equal('La tabla se creó correctamente.');    });    it('should return 400 if teacher name is missing', async () => {      const res = await request(app)        .get('/teacher-schedules/create-teacher-table');      expect(res.statusCode).to.equal(400);      expect(res.body.success).to.equal(0);      expect(res.body.message).to.equal("El parámetro 'profe' es requerido.");    });    it('should return 500 if table creation fails', async () => {      mockClient.query.throws(new Error('DB Error'));      const res = await request(app)        .get('/teacher-schedules/create-teacher-table?profe=ProfesorA');      expect(res.statusCode).to.equal(500);      expect(res.body.success).to.be.false;    });  });  describe('POST /teacher-schedules/delete-all-schedules', () => {    it('should delete all schedules successfully', async () => {      mockClient.query.withArgs("SELECT table_name FROM information_schema.tables WHERE table_schema='horarios_profes';").resolves({ rows: [{ table_name: 'table1' }, { table_name: 'table2' }] });      mockClient.query.withArgs('DROP TABLE IF EXISTS table1').resolves({});      mockClient.query.withArgs('DROP TABLE IF EXISTS table2').resolves({});      const res = await request(app)        .post('/teacher-schedules/delete-all-schedules');      expect(res.statusCode).to.equal(200);      expect(res.text).to.equal('Todas las tablas han sido eliminadas exitosamente.');    });    it('should return 500 if deleting all schedules fails', async () => {      mockClient.query.withArgs("SELECT table_name FROM information_schema.tables WHERE table_schema='horarios_profes';").throws(new Error('DB Error'));      const res = await request(app)        .post('/teacher-schedules/delete-all-schedules');      expect(res.statusCode).to.equal(500);    });  });  describe('POST /teacher-schedules/delete-schedule/:name', () => {    it('should delete a schedule successfully', async () => {      mockClient.query.resolves({});      const res = await request(app)        .post('/teacher-schedules/delete-schedule/ProfesorA');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.true;      expect(res.body.message).to.equal("Tabla 'ProfesorA' eliminada exitosamente");    });    it('should return 400 if name is missing', async () => {      const res = await request(app)        .post('/teacher-schedules/delete-schedule/');      expect(res.statusCode).to.equal(404);     });    it('should return 500 if schedule deletion fails', async () => {      mockClient.query.throws(new Error('DB Error'));      const res = await request(app)        .post('/teacher-schedules/delete-schedule/ProfesorA');      expect(res.statusCode).to.equal(500);      expect(res.body.success).to.be.false;    });  });  describe('GET /teacher-schedules/get-account-schedule-record', () => {    it('should return account schedule record successfully', async () => {      mockClient.query.resolves({ rows: [{ table_name: 'profesora' }] });      const res = await request(app)        .get('/teacher-schedules/get-account-schedule-record?name=ProfesorA');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.true;      expect(res.body.data).to.be.an('array').that.is.not.empty;    });    it('should return 400 if name is missing', async () => {      const res = await request(app)        .get('/teacher-schedules/get-account-schedule-record');      expect(res.statusCode).to.equal(400);    });    it('should return 404 if no tables match the name', async () => {      mockClient.query.resolves({ rows: [] });      const res = await request(app)        .get('/teacher-schedules/get-account-schedule-record?name=nonExistent');      expect(res.statusCode).to.equal(404);      expect(res.body.success).to.be.false;      expect(res.body.message).to.equal('No hay tablas que coincidan con el nombre proporcionado');    });    it('should return 500 if fetching account schedule record fails', async () => {      mockClient.query.throws(new Error('DB Error'));      const res = await request(app)        .get('/teacher-schedules/get-account-schedule-record?name=ProfesorA');      expect(res.statusCode).to.equal(500);    });  });  describe('GET /teacher-schedules/get-schedule-record', () => {    it('should return schedule records successfully', async () => {      mockClient.query.resolves({ rows: [{ table_name: 'table1' }] });      const res = await request(app)        .get('/teacher-schedules/get-schedule-record');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.true;      expect(res.body.data).to.be.an('array').that.is.not.empty;    });    it('should return success false if no records are found', async () => {      mockClient.query.resolves({ rows: [] });      const res = await request(app)        .get('/teacher-schedules/get-schedule-record');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.false;      expect(res.body.message).to.equal('No se encontraron reservations para el ID proporcionado');    });    it('should return 500 if fetching schedule record fails', async () => {      mockClient.query.throws(new Error('DB Error'));      const res = await request(app)        .get('/teacher-schedules/get-schedule-record');      expect(res.statusCode).to.equal(500);    });  });  describe('GET /teacher-schedules/view-schedule', () => {    it('should return schedule view successfully', async () => {      mockClient.query.resolves({ rows: [{ horas: '8:00 - 9:00', Lunes: 'Math' }] });      const res = await request(app)        .get('/teacher-schedules/view-schedule?name=ProfesorA');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.true;      expect(res.body.data).to.be.an('array').that.is.not.empty;    });    it('should return 400 if name is missing', async () => {      const res = await request(app)        .get('/teacher-schedules/view-schedule');      expect(res.statusCode).to.equal(400);    });    it('should return No_hay_registros if no records are found', async () => {      mockClient.query.resolves({ rows: [] });      const res = await request(app)        .get('/teacher-schedules/view-schedule?name=nonExistent');      expect(res.statusCode).to.equal(200);      expect(res.body.data).to.equal('No_hay_registros');    });    it('should return 500 if viewing schedule fails', async () => {      mockClient.query.throws(new Error('DB Error'));      const res = await request(app)        .get('/teacher-schedules/view-schedule?name=ProfesorA');      expect(res.statusCode).to.equal(500);    });  });  describe('GET /teacher-schedules/check-existence', () => {    it('should return existence status successfully', async () => {      mockClient.query.resolves({ rows: [{ table_name: 'profesora' }] });      const res = await request(app)        .get('/teacher-schedules/check-existence?profe=ProfesorA');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.true;      expect(res.body.data).to.be.an('array').that.is.not.empty;    });    it('should return 400 if profe is missing', async () => {      const res = await request(app)        .get('/teacher-schedules/check-existence');      expect(res.statusCode).to.equal(400);    });    it('should return success false if no records are found', async () => {      mockClient.query.resolves({ rows: [] });      const res = await request(app)        .get('/teacher-schedules/check-existence?profe=nonExistent');      expect(res.statusCode).to.equal(200);      expect(res.body.success).to.be.false;      expect(res.body.message).to.equal('No se encontraron reservations para el ID proporcionado');    });    it('should return 500 if checking existence fails', async () => {      mockClient.query.throws(new Error('DB Error'));      const res = await request(app)        .get('/teacher-schedules/check-existence?profe=ProfesorA');      expect(res.statusCode).to.equal(500);    });  });});